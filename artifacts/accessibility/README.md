# Accessibility Reports

This directory stores the JSON and HTML artefacts generated by `npm run test:accessibility`. Each report captures the axe-core scan results for a specific client shell state so Playwright and CI runs can diff accessibility regressions over time.
